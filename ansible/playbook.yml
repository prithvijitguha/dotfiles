- hosts: local

  vars_files:
    - vars.yml

  tasks:

    ########################################################
    # APT BASE
    ########################################################

    - name: Update apt cache
      apt:
        update_cache: true
        cache_valid_time: 3600

    ########################################################
    # INSTALL BASE APT PACKAGES
    ########################################################

    - name: Install apt packages
      apt:
        name: "{{ apt_packages }}"
        state: present
        update_cache: true

    ########################################################
    # INSTALL CHEZMOI
    ########################################################

    - name: Install chezmoi
      community.general.snap:
        name: chezmoi
        classic: true

    ########################################################
    # INSTALL NODE.JS + NPM
    ########################################################

    - name: Install Node.js
      shell: |
        curl -fsSL https://deb.nodesource.com/setup_lts.x | bash -
        apt install -y nodejs
      args:
        creates: /usr/bin/node

    ########################################################
    # INSTALL GITHUB CLI
    ########################################################

    - name: Install GitHub CLI
      shell: |
        (type -p wget >/dev/null || (apt update && apt install wget -y)) \
        && mkdir -p -m 755 /etc/apt/keyrings \
        && out=$(mktemp) && wget -nv -O$out https://cli.github.com/packages/githubcli-archive-keyring.gpg \
        && cat $out | tee /etc/apt/keyrings/githubcli-archive-keyring.gpg > /dev/null \
        && chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg \
        && mkdir -p -m 755 /etc/apt/sources.list.d \
        && echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | tee /etc/apt/sources.list.d/github-cli.list > /dev/null \
        && apt update \
        && apt install gh -y
      args:
        creates: /usr/bin/gh

    ########################################################
    # NPM GLOBALS
    ########################################################

    - name: Install npm global packages
      community.general.npm:
        name: "{{ item }}"
        global: true
      loop: "{{ npm_packages }}"

    ########################################################
    # UV
    ########################################################

    - name: Install uv
      shell: |
        curl -LsSf https://astral.sh/uv/install.sh | sh
      become: false
      args:
        creates: "{{ real_home }}/.local/bin/uv"

    ########################################################
    # RUSTUP / CARGO
    ########################################################

    - name: Install rustup and cargo
      shell: |
        curl https://sh.rustup.rs -sSf | sh -s -- -y
      become: false
      args:
        creates: "{{ real_home }}/.cargo/bin/rustup"

    ########################################################
    # OH MY ZSH
    ########################################################

    - name: Install Oh My Zsh
      git:
        repo: https://github.com/ohmyzsh/ohmyzsh.git
        dest: "{{ real_home }}/.oh-my-zsh"
        depth: 1
      become: false

    ########################################################
    # POWERLEVEL10K
    ########################################################

    - name: Clone Powerlevel10k
      git:
        repo: https://github.com/romkatv/powerlevel10k.git
        dest: "{{ real_home }}/powerlevel10k"
        depth: 1
      become: false

    ########################################################
    # SET DEFAULT SHELL
    ########################################################

    - name: Set zsh as default shell
      user:
        name: "{{ real_user }}"
        shell: /usr/bin/zsh

    ########################################################
    # VIM-PLUG
    ########################################################

    - name: Create Vim autoload directory
      file:
        path: "{{ real_home }}/.vim/autoload"
        state: directory
        mode: '0755'
      become: false

    - name: Install vim-plug for Vim
      get_url:
        url: https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
        dest: "{{ real_home }}/.vim/autoload/plug.vim"
        mode: '0644'
      become: false

    ########################################################
    # TPM (TMUX PLUGIN MANAGER)
    ########################################################

    - name: Install TPM
      git:
        repo: https://github.com/tmux-plugins/tpm
        dest: "{{ real_home }}/.tmux/plugins/tpm"
        depth: 1
      become: false

    ########################################################
    # NERD FONT - JETBRAINS MONO
    ########################################################

    - name: Create fonts directory
      file:
        path: "{{ real_home }}/.local/share/fonts"
        state: directory
        mode: '0755'
      become: false

    - name: Download JetBrainsMono Nerd Font
      get_url:
        url: https://github.com/ryanoasis/nerd-fonts/releases/latest/download/JetBrainsMono.zip
        dest: /tmp/JetBrainsMono.zip

    - name: Extract JetBrainsMono Nerd Font
      unarchive:
        src: /tmp/JetBrainsMono.zip
        dest: "{{ real_home }}/.local/share/fonts"
        remote_src: true
      become: false

    - name: Refresh font cache
      shell: fc-cache -fv
      become: false
      changed_when: false
